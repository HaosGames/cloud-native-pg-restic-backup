name: CI/CD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.21'

    - name: Install Restic
      run: |
        sudo apt-get update
        sudo apt-get install -y restic

    - name: Build
      run: make build

    - name: Run tests
      run: make test
      env:
        # Tests will be skipped without these environment variables
        TEST_RESTIC_REPOSITORY: /tmp/test-restic-repo
        TEST_RESTIC_PASSWORD: abcdefg

  build-and-push:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Login to Docker Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io/haosgames
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.GITHUB_TOKEN }}

    - name: Build and push Docker image
      run: |
        make docker-build PLUGIN_IMAGE=${{ secrets.DOCKER_REGISTRY }}/cloud-native-pg-restic-backup
        make docker-push PLUGIN_IMAGE=${{ secrets.DOCKER_REGISTRY }}/cloud-native-pg-restic-backup
